package components

css container() {
    display: flex;
    flex-direction: column;
    min-height: 100vh;
}

templ Container() {
    <div class={ container() }>
        { children... }
    </div>
}
